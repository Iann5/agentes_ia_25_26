# Tarea CURL
 - Crear un documento que ejemplifique todas las posibles opciones del comando CURL
 - Usar alguna web facke rest api para probar las posibles funcionalidades del comando CURL
 - Crear /scripts/test.sh que lance toda los posibles test del comando curl para realizar 
   GET, POST, DELETE, PATCH, PUT
